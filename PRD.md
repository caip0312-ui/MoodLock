# MoodLock 产品需求文档 (PRD) v22

> 轻量级 PRD,供开发时给 AI 编程助手(Claude Code)提供持续参考,
> 也作为项目的产品设计记录。每次想法有重大变化,更新这份文档,而不是只在对话里口头说。
> v2 更新:心情档位改为 5 档;语音录音模块改为可选开关;去除锁屏内 Face ID 验证方案,
> 改用"待确认"机制;明确技术风险验证项;明确视觉风格方向;明确开发优先级。
> v3 更新:确定语音录音默认关闭;明确 5 档心情的配色方向(冷暖两极+灰色中性,不做拟人表情);
> 明确 MVP 不做用户自定义配色;确定月度图表方案为"日历热力图(每天一个色块)+ 点击展开当天时间线",
> 解决一天多次记录导致月视图拥挤的问题,已产出可交互示意稿(见 mood_chart_mockup.html)。
> v4 更新:确定心情选择器的交互形状——5 个等大圆点,配色沿用双极配色,不做拟人表情;
> 明确"数据层"（决定图表能不能算平均值的颜色数值）和"视觉皮肤层"（按钮好不好看、要不要涂鸦/照片）要分开,
> MVP 阶段按钮只用纯色圆点,不做自由涂鸦、不做照片上传;把涂鸦/照片皮肤、预设配色主题等个性化想法
> 整理进第 5.1 节"个性化皮肤(远期)",与第 5 节的 AI 洞察/HealthKit/watchOS 一起作为后续版本清单管理。
> v5 更新:明确涂鸦皮肤的具体形态(底色上叠加简笔画装饰、不遮住底色);补充配色的科学依据和跨文化
> 颜色情绪关联研究,确认保留蓝配橙的双色系方案,并记录了两个可替换的备选配色组合供以后参考。
> v6 更新:确定皮肤层不需要单独建 MoodSkin 表——按"每条记录单独装饰"还是"某个档位整体换皮肤"
> 两种场景分别给了轻量方案(前者是 MoodEntry 上加可空字段,后者是用户设置里加一个映射),都不涉及
> 独立关系表;明确所有图表(含新增的分布直方图)永远只读心情档位数值,皮肤层不参与任何分析,
> 因此皮肤层不需要对应的图表类型;新增"本月心情分布"直方图设计,已加入 mood_chart_mockup.html。
> v7 更新:补充总体设计原则(前端尽量简化、后端/数据结构尽量完善留有扩展空间);澄清 skinImageURL、
> doodleData 不是独立的表,是 MoodEntry 表里的两个可选字段;明确"档位皮肤可随时更改"对应的是
> 用户设置里的一个可更新映射(类似微信背景图),不需要额外结构;并记录了一个需要产品侧做决定的分支——
> 换皮肤后历史记录要不要跟着变(微信背景图式,简单)还是保留记录当下生效的皮肤快照(需要用到
> MoodEntry 上的可选字段,略复杂)。
> v8 更新:确定历史记录**不跟着变**——换皮肤后,过去的记录继续显示记录当下生效的皮肤,不会被
> 后来的更换覆盖。这意味着 skinImageURL / doodleData 不只是"用户手动装饰"用的字段,还要承担
> "记录产生的那一刻,自动把当时 UserSettings.moodLevelSkins 里对应档位的当前皮肤复制一份存进来"
> 这个自动快照职责,两者共用同一对字段,写入时机不同(手动装饰 vs 自动快照),开发时需要分清楚。
> v9 更新:第 6 节两条关键技术风险已完成验证并结案(在 iOS 26 / WWDC25 最新资料下复核过,结论未变),
> 正式进入开发阶段,详见第 6 节的验证结论和第 12 节的开发顺序建议。
> v10 更新:重新设计第 7 节的"待确认"机制——原方案要求用户逐条确认/撤销,对只用锁屏记录、
> 很少打开 App 的用户反而会造成"待办堆积"的负担,与轻量化定位相悖。改为两种可选的轻量方案,
> 待正式开发前拍板一种(默认建议直接取消确认门槛,见第 7 节)。
> v11 更新:**发现一条新的关键技术限制,已加入第 6 节**——锁屏 Widget 的内容被系统强制渲染成单一
> 色调("tinted for legibility"),不是开发者可以配置的,意味着双极配色在锁屏上完全不生效,只有
> 桌面小组件(默认全彩,除非用户自己开启系统级的"色调"外观模式)和 App 内部能正常显示彩色。
> 因此把非颜色的区分手段(触感反馈、弧线轨道+位置/大小)从"可选加分项"提升为"锁屏场景下的
> 必需设计",相关内容已写入第 8 节并产出对比示意图(lockscreen_vs_homescreen.png)。
> v12 更新:采纳"月牙/嘴角造型"方案作为心情图标的主造型——两端(很难过/很开心)细月牙、
> 越靠近"一般"越饱满、"一般"是实心圆,靠形状本身区分档位,不依赖颜色,天然适配锁屏强制单色
> 渲染的限制,已产出示意图(crescent_icons.png)。同时新增一条待验证的技术风险(见第 6 节):
> 锁屏小组件槽位的实际可用尺寸能否容纳 5 个满足最小点击面积的独立图标,尚未验证。
> v13 更新:明确锁屏、桌面小组件、App 内**统一使用同一套图标设计**——颜色圆点之上叠加月牙
> 遮罩,不是"锁屏一套月牙方案、桌面/App 一套纯色圆点方案"两套并存;锁屏被系统强制去色时,
> 月牙形状依然生效,桌面/App 内颜色不受影响、和形状同时可见,双重编码更稳健(见第 8 节)。
> 明确 App 内**不做**连续滑轮式(色轮式)的精细情绪输入,继续用 5 档离散值,避免图表和数据结构
> 出现两套精度并存的复杂度。新增一条极早期的远期想法(第 5 节)——"全球心情广场",记录下来是
> 因为它明确要求图标保留颜色而非纯黑白/纯形状,和第 8 节的设计判断直接相关,但依赖云同步,与
> 本版本"不做云同步"的范围直接冲突,真要做需要重新评估整个隐私模型,现阶段只记录想法不展开。
> v14 更新:调整第 11 节"开放问题"的管理方式——不再用删除线保留已解决的问题,已解决的事项直接
> 从第 11 节删除(决策本身已经写进正文对应章节),第 11 节只保留当前真正待拍板的事项;完整的历史
> 演变仍由本变更记录一层承接,避免"开放问题"和"决策历史"混在一起、越积越长、难以一眼看出真正
> 待办的问题。
> v15 更新:第 7 节误操作处理机制正式拍板——采用方案 A(取消确认门槛),同时新增一层纯本地的
> 异常检测规则(60 秒内 ≥3 条记录且出现 ≥2 个不同心情档位则标记为疑似异常),命中规则只触发历史
> 列表顶部的被动提示条,不阻断记录写入、不影响图表统计。数据结构里原本待定的 confirmed 字段替换
> 为 flaggedAnomalous 字段(见第 9 节);第 11 节因此清空,第 12 节开发顺序第 3 步同步更名为
> "异常检测与被动提示"。
> v16 更新:撤销第 5.1 节"每条记录自动快照当时生效皮肤"的设计——档位皮肤改回历史记录跟着变
> (微信背景图式),换皮肤后过去的记录也统一按新皮肤展示,不再需要在写入 MoodEntry 时额外做一次
> 快照复制。MoodEntry 上的 skinImageURL / doodleData 字段只保留"用户手动为单条记录单独装饰"这
> 一个用途,第 9 节数据结构描述同步更新。
> v17 更新:修正第 8.1 节"按天取平均"掩盖情绪起伏的问题——一天内如果记了两条截然相反的心情,
> 平均值会显示成和真正平静的一天完全相同的"一般",这一眼本身会骗人,光靠"可以点进去看详情"补救
> 不了。改法:日历热力图的色块从单一平均色改成"当天最低→最高档位"的线性渐变,趋势线在每个点上
> 加一条当天最低到最高的细竖线(类似 K 线图的高低线),波动小的日子自动退化成和原来一样的效果,
> 只有真正大幅摆动的日子才会明显显现;同时新增一个可选的深度视图——心情时刻分布散点图,不做任何
> 按天概括,把每条原始记录按实际时间点单独摆出来。以上都不改变第 9 节的数据结构。
> v18 更新:正式采纳"情绪互动装置"(软糖圆球)——App 内主页新增一个会随当前心情变色的球体,材质做
> 成软糖质感、不画五官,作为记录之后的解压/正反馈元素,写进第 4 节和第 8 节,并划定严格边界:只在
> App 主页出现(不上锁屏/桌面小组件)、不改变月牙造型的心情输入方式、不产生也不修改任何记录,纯
> 皮肤层。这是本 MVP 第一次也是唯一一次主动引入装饰性元素,第 4/8 节都加了例外说明,避免和"不做
> 拟人化/装饰角色"的原则混淆。同时顺手修正了第 3 节一处过时描述(还停留在已废弃的"待确认"机制
> 措辞,未跟上 v15 改成方案 A + 异常检测的结论)。
> v19 更新:**第 6 节"锁屏小组件槽位尺寸"从"待验证"变成"已验证"**——查到苹果官方 HIG "Widgets"
> 页面的 Specifications 表格,拿到了锁屏 accessory widget 在每种 iPhone 屏幕尺寸下的官方点数(pt)
> 尺寸,不再是猜测。结论:5 个独立可点击的月牙图标横向平分锁屏矩形槽位后,单个点击区域约
> 30–34pt,在所有当前 iPhone 机型上都达不到苹果建议的 44×44pt 最小点击面积,这是确定存在的架构性
> 限制。原来的"待办:让 Claude Code 做原型实测尺寸"已经不需要了(官方数据已经给出精确数字);
> 但"5 档要不要拆、怎么拆"变成一个需要拍板的新开放问题,已移入第 11 节。
> v20 更新:**实际用了一段时间后集中暴露出 12 处问题,逐条拍板后一次性写入本次更新**,不再零散
> 改动,避免像 v18 软糖那次一样和另一个窗口的并行改动打架。主要决定:①锁屏正式定为 3 档粗粒度
> (难过/一般/开心),细分的 5 档(含两端"很难过""很开心")只在桌面小组件和 App 内保留,不再是
> 开放问题(第 11 节对应条目已删除,结论写进第 6 节);②桌面小组件改用长矩形(Medium)布局,
> 而不是正方形,5 档不再拥挤;③主页底色正式确定跟随当前心情颜色动态变化(和软糖圆球一起变),
> 但底色取值要和图标/图表用的心情色明确区分饱和度或明度,避免"同一个颜色=同一个数据含义"的
> 编码规则被"背景也是这个颜色"的巧合破坏;④确定要做一次覆盖设置/历史/趋势等二级页面的统一
> 视觉规范,并把这些页面的文字导航入口改成图标形式集中放置,和主页视觉语言统一;⑤第 7 节新增
> "十分钟内同一心情连续点击自动归并为一条记录"的规则,应对实际使用中出现的"报复性解压式重复
> 点击"场景,这是和 v13 被否决的"多次点击表示强度"方案相关的同一类数据完整性问题在真实使用中的
> 再次出现,这次选择"归并"而不是"编码强度";⑥新发现一条技术限制并写入第 6 节——WidgetKit 的
> 交互式 Widget(锁屏/桌面)点击目前没有官方支持的震动反馈 API,只有 App 内(月牙选择器、软糖圆球)
> 能可靠触发标准震动;⑦月牙纯形状编码的可学习性问题——决定不放弃形状编码(锁屏强制单色的限制
> 依然成立,形状仍是锁屏场景下唯一可靠的区分手段),但在不受空间和强制单色限制的场景(桌面小
> 组件、App 内)恢复显示文字档位标签,降低"看不出月牙代表哪一档"的学习成本,锁屏保持纯形状+
> 触感,App 内和桌面小组件"形状+颜色+文字"三重编码;⑧App 内不再重复显示"MoodLock"这个 App
> 名称;⑨新增"设计 App 图标"这一项到第 4 节功能范围。
> v21 更新:**根据真实使用数据做了一次范围收缩——语音录音模块整体移除,历史记录列表重新定义为
> "只列有备注的时刻"。** 起因:实际使用下来,一天可能产生几十条心情点击,但真正会补充详细内容
> (备注/语音)的只有个别几条,原来"历史记录列表按日期列出所有记录"的设计,会被大量没有任何
> 补充信息的裸记录淹没,而这些裸记录的"数量和分布"已经由第 8.1 节的热力图/趋势线/分布直方图
> 处理得更好,历史列表继续列全部记录属于和图表功能重复,却没有图表做得好。处理方式:①**语音
> 录音模块整体移除**(不只是关掉开关,是整个功能连同 `audioURL`、`transcribedText` 字段、
> `UserSettings.voiceRecordingEnabled` 开关一起删除),文字备注保留,因为文字备注的实现和维护
> 成本远低于语音(不需要麦克风权限、音频文件存储、语音转文字管线),没有理由因为使用率低就一起
> 砍掉;②**历史记录列表重新定义为"只展示带文字备注的记录"**,不再是"按日期列出全部记录"的
> 原始流水账,变成一份真正有内容可回看的轻量日记,原来"按日期查看全部记录"这个需求由第 8.1 节
> 热力图点进去的当天时间线视图承接,不需要历史列表重复做一遍;③第 3/4/7/9/12 节所有提到语音
> 录音的地方同步删除或改写。
> v22 更新:**纠正 v21 对"历史列表"的一处理解偏差。** v21 把历史列表改成"只展示带备注的记录",
> 但用户的原意是**全部原始记录都继续保留、可以看到**,只是想要给"带备注的重要时刻"加一种突出
> 显示的方式,不是把没备注的记录过滤掉、藏起来。改回:**历史列表默认列出全部原始记录**(按时间
> 倒序,和 v20 之前的行为一致),带文字备注的记录在列表里做视觉突出——具体做法是加一个小的备注
> 图标/角标 + 该条备注的一行预览文字,而不是用新的颜色去突出,因为第 8 节已经把颜色完全用来编码
> 心情档位,如果突出显示也用颜色,会破坏"颜色只代表心情"这条既有规则,容易让人误以为高亮色又是
> 一种新的数据含义;没有备注的记录只显示心情图标和时间,视觉上更轻。第 9 节数据结构不需要变
> 化——`note` 字段是否为空本来就足够驱动这个高亮判断,不需要新增字段。

- 现有心情记录类 App 大多需要打开 App、进入某个页面才能记录心情,操作步骤多,容易被遗忘或放弃。
- 智能手表已经能通过生理信号(心率、HRV)推断"唤醒度",但无法判断"效价"(是开心还是难过)以及"为什么"——主观自评加上下文信息仍然有独特价值。
- 苹果系统自带 State of Mind 功能、以及 App Store 上已有的 Lockscreen Widget 类 App,已经覆盖了"锁屏快速打点+趋势图"这个基础需求,本项目的差异化不在记录方式本身,而在记录之后的 AI 洞察层(见第 5 节)。
- 目标:把"记录当下心情"的操作成本降到最低,同时保留"愿意深入记录"的空间,并在数据积累后提供比现有产品更有洞察力的分析。

## 1.1 总体设计原则

- **前端尽量简化、方便**:日常打开看到的东西要尽量少而直接(锁屏几个圆点、App 内几个清爽的页面),不给用户增加多余的操作步骤和视觉噪音
- **数据结构/后端尽量完善,留有扩展空间**:界面简单不等于底层设计粗糙,数据结构要为后续想加的功能(皮肤、AI 洞察、HealthKit)预留合理的字段和边界,避免以后每加一个功能都要推倒重来。具体做法是:所有分析类功能(图表)统一只读心情档位这一个基础数值,不直接依赖任何"外观类"数据(皮肤、涂鸦、录音),外观类数据作为独立的可选字段或设置项挂在旁边,不侵入核心数据结构

## 2. 目标用户

- 第一阶段:我自己(作为日常心情记录习惯的验证者)
- 后续可扩展:有轻量情绪自我觉察需求、但不想用复杂心理健康 App 或装饰性日记 App 的用户

## 3. 核心用户旅程

1. 用户在锁屏界面(或桌面 Widget)看到心情记录入口:5 档心情选择区域
2. 点击对应档位,系统记录:时间戳 + 心情等级 + 来源(锁屏/桌面/App 内)
   - 不设确认门槛,直接存入历史;若命中第 7 节的本地异常检测规则(短时间内出现多个不同档位),标记为疑似异常,下次打开 App 时历史列表顶部会有一条被动提示(见第 7 节隐私与误操作处理)
3. 当天晚些时候用户打开 App,可以在第 8.1 节热力图点进某一天的时间线视图里,给某一条记录补充文字备注(语音录音模块已在 v21 移除,详见第 4 节)
4. 用户可以在 App 内查看"历史"(全部原始记录按时间倒序列出,带文字备注的记录会有备注图标角标+一行预览文字突出显示,方便一眼找到"认真写过点什么的时刻")、周/月汇总图表、心情趋势折线图
5. (二期)积累一段时间数据后,App 生成自然语言的周度心情观察报告,结合日历密度/睡眠/天气等上下文

## 4. 功能范围(本版本 MVP)

**包含:**
- [ ] App 内 5 档心情按钮 + 时间戳记录,数据本地存储
- [ ] **历史记录列表页,展示全部原始记录(按时间倒序),带文字备注的记录用图标角标+备注预览文字突出显示(v22 修正,v21 一度误改成"只展示带备注的记录"被纠正回来)。** 一天可能产生几十条心情点击,列表不因此被过滤,但靠这层突出显示,"认真写过点什么的时刻"能一眼从大量裸记录里被找出来,不需要逐条点开;数量/分布类的问题继续交给第 8.1 节的图表(热力图/趋势线/分布直方图/点击展开当天时间线)回答,两者不冲突
- [ ] 点击某条记录可补充文字备注(v21 起,这是记录之后唯一的深度补充手段,语音录音已移除)
- [ ] 锁屏 Widget:3 档粗粒度心情记录入口(难过/一般/开心,具体原因见第 6 节技术风险)
- [ ] 桌面 Widget:5 档心情记录入口,采用长矩形(Medium)布局,不用正方形(见第 6 节)
- [ ] 周/月心情汇总图表 + 趋势折线图
- [ ] App 内主页的情绪互动装置(软糖质感圆球,具体范围和边界见第 8 节)
- [ ] App 图标设计
- [ ] 设置/历史/趋势等二级页面的统一视觉规范改版,文字导航入口改为图标形式集中放置(见第 8.2 节)

**明确不做(本版本):**
- [ ] 云端同步 / 多设备
- [ ] 账号系统 / 登录
- [ ] 社交分享
- [ ] 语音录音(v21 已移除整个模块,原因见版本变更记录——实际使用率太低,不值得维持麦克风权限、音频存储、转文字管线这套工程量,文字备注保留)
- [ ] 语音/人脸情绪识别(自动判断情绪,和上面移除的"用户主动录音"是完全不同的两件事,这里指的是自动推断,本来就不在范围内)
- [ ] 装饰性日记功能(贴纸、照片拼贴、字体美化等——这是另一类产品的方向,不是本项目要竞争的点);**唯一的例外是第 8 节新增的"情绪互动装置"软糖圆球,范围和理由见第 8 节,不代表本项目转向装饰系**
- [ ] 通知提醒(可作为未来迭代项)

## 5. 二期功能 / 差异化重点(先记录想法,暂不在 MVP 开发)

> 这是本项目相对市面产品(Daylio、Bearable、苹果原生 State of Mind、Lockscreen Widget 类 App)
> 真正的差异化机会所在,建议在 MVP 稳定后优先投入,而不是被 HealthKit/watchOS 这类"看起来更硬核"
> 但实际是平台工程量而非 AI 含量的功能分散精力。

- 周报生成:结构化数据聚合(心情分布 + 可选的日历密度/睡眠/天气)后,调用 LLM API 生成自然语言心情观察报告
  - 要求:只描述观察到的关联性,不下诊断、不做武断因果判断
  - 技术上是这几项里门槛最低的(一次 API 调用 + 提示词设计),优先级应高于下面两项
- HealthKit 接入(睡眠、心率数据),与心情记录做关联展示——标准 SDK 集成,AI 辅助下可行,但需要真机验证
- watchOS 独立 App——工程量明显更大(独立目标、独立权限、后台限制更严格),需要实体 Apple Watch 测试,列为远期 stretch goal,不纳入近期规划
- **全球心情广场(极早期想法,仅记录,不纳入任何近期规划)**:参考同类 App 里"把这一刻所有用户的心情图标汇聚成一屏"的展示方式(用户提供过类似截图作参考),用满屏彩色小图标让人一眼感受"此刻大家整体的情绪氛围"。这个想法**前提是要有云端账号和数据同步**,和第 4 节明确写的"本版本不做云同步/账号系统"直接冲突,意味着一旦真的要做,需要重新评估整个隐私和数据存储模型(第 7 节),不是加一个页面那么简单。之所以现在就记下来,是因为它直接影响了第 8 节"心情图标要不要保留颜色"这个判断——如果未来有这个方向,图标设计就必须保留颜色(而不是纯黑白/纯形状),因为"一眼看氛围"这种多图标场景下颜色的辨识效率远高于形状细节

## 5.1 个性化皮肤(远期,不纳入近期规划)

> 这些想法都属于"按钮好不好看"的视觉皮肤层,和第 9 节数据结构里真正驱动图表的颜色数值
> 是分开的两件事——皮肤层怎么改,底层的心情数值编码都不能变,否则第 8.1 节的月度色块平均值
> 计算逻辑会失效。做这层之前,必须先确认清楚"皮肤"和"数据"如何解耦存储。

- 预设配色主题:提供 2-3 套设计好的配色方案让用户选,而不是完全开放的自定义调色盘(见第 8 节的理由)
- 用户涂鸦作为心情按钮的装饰:具体形态是在颜色圆点**之上**叠加一层简笔画式的线条装饰(比如画一个简单的笑脸轮廓),不覆盖、不遮住底色,颜色圆点本身依然完整可见并参与图表计算;这是"贴纸挂件"式的装饰,不是替换整个按钮的视觉语义
- 自定义照片作为按钮:用户可上传照片(或使用同一张照片作为长期固定按钮),同样作为颜色圆点之外的叠加装饰,而不是取代颜色编码;月度汇总图表始终按底层心情数值渲染,不会因为按钮换了皮肤而变成一堆无法平均的照片拼贴
- 这几项都需要额外的图片选择器权限申请、图片裁剪压缩、存储管理等工程量,评估优先级时应排在第 5 节的 AI 洞察功能之后
- **数据结构上不需要单独建 MoodSkin 表**。skinImageURL、doodleData 不是独立的表,而是 MoodEntry 这一张表里额外加的两个**可选字段**(大部分记录留空,只有用户主动装饰某条记录时才有值)。按皮肤的作用范围分两种轻量方案:
  - 若皮肤是"每条记录各自独立的装饰"(比如这条开心记录配笑脸、那条开心记录配另一个图案):就用 MoodEntry 上 skinImageURL / doodleData 这两个字段,因为这份装饰只属于这一条记录,不会被复用
  - 若皮肤是"某个心情档位整体可随时切换的样子"(比如"开心"这个档位可以随时换成不同的照片,类似微信背景图随时可换):放进用户设置里做一个"档位 → 当前皮肤"的映射即可,完全不需要碰 MoodEntry 表——这个映射本身就是"当前值",改一次就整体生效,天然支持随时更改,不需要额外设计"可编辑"的机制
  - **已改为:历史记录跟着变(微信背景图式)。** 撤销了之前"每条记录自动快照当时生效的皮肤"这个设计——不再需要在记录产生的那一刻做任何自动复制动作。档位皮肤就是 UserSettings.moodLevelSkins 里的一份"当前值",换了皮肤之后,所有历史记录(不管是之前记的还是以后记的)展示时都统一读取这份当前值,过去的记录也会跟着变成新皮肤的样子,不保留旧皮肤的痕迹。好处是实现更简单——不需要在每次写入 MoodEntry 时多做一步"复制快照"的动作,少一个写入路径,也不用担心自动快照和用户手动装饰互相覆盖的问题。MoodEntry 上的 skinImageURL / doodleData 这两个字段因此只保留"用户手动为单条记录单独装饰"这一个用途(比如只想给某一条记录配一张特定照片),不再承担自动快照职责
  - 不管选哪种,前提都是所有分析图表(热力图、趋势线、分布直方图)永远只读 moodLevel 这个基础数值,不读皮肤字段——这条规则本身就让"皮肤层要不要配一种专门的图表"这个问题不存在,不需要额外设计

## 6. 关键技术风险(已验证结案)

- **锁屏交互形式:已确认结论。** 交互式 Widget(iOS 17 起)只支持 `Button` 和 `Toggle` 两种控件、必须绑定 `AppIntent`,不支持连续手势(`DragGesture` 等在 Widget 里挂不上有效追踪)——这是架构层面的限制(Widget 是半静态的快照渲染,不承载完整手势识别系统),不是文档遗漏或版本差异。已核对 WWDC25(iOS 26)最新资料,今年的 Widget 更新集中在视觉呈现和跨端支持,手势能力没有变化,结论稳定。
  - **最终方案:5 个固定点位、分段点击**,视觉上模拟滑轨/滑块效果,交互上是点选,这是目前唯一可行的技术路径,正式采用
  - **重要澄清**:锁屏 Widget 本身是可以交互点击的,Button/Toggle 能在锁屏上正常触发 AppIntent 执行后台逻辑,"不能连续滑动"和"完全不能点"是两回事,不要过度保守地以为锁屏 Widget 整体不可交互
- **锁屏内生物识别验证:已确认不可行,原因是架构性的、非实现细节。** `LocalAuthentication`(Face ID/Touch ID)只能在前台运行的 App 或授权的系统级上下文里发起,Widget 的 AppIntent 运行在受限的后台执行环境,没有能力弹出生物识别确认弹窗;唯一路径是打开完整 App,而"打开 App"这个动作在锁屏状态下本身就等同于解锁,和"验证但不解锁"的目标直接矛盾,逻辑上无解
  - 最终方案:采用第 7 节"取消确认门槛 + 本地异常检测"的机制,不再考虑锁屏内生物识别方案
- **新发现:锁屏 Widget 的内容被系统强制渲染成单一色调,颜色区分在锁屏上完全失效。** 苹果对锁屏 Widget 有明确规则——不论开发者在代码里设置了什么颜色,系统都会统一套上单色调渲染("tinted for legibility"),目的是保证在任何壁纸背景下都清晰可读,这是系统层面的强制规则,不是可以关闭或配置的选项。这意味着第 8 节的双极配色方案(蓝→灰→橙)在锁屏场景下会全部显示成同一个颜色,只是深浅/大小可能有差异,"用颜色区分心情"这件事在锁屏上根本不成立
  - 对比之下,**桌面主屏幕小组件默认是全彩的**,不受此限制,只有当用户自己在系统设置里开启"色调"这种主屏幕外观模式(iOS 18 起新增的可选项,类似深色模式的系统级开关,影响所有 App 的图标和小组件,不是 MoodLock 单独可控的)时才会变成单色
  - 最终方案:心情图标在三端使用同一套"颜色圆点 + 月牙遮罩"设计(见第 8 节),不是锁屏和桌面/App 各做一套;锁屏场景下系统自动去色,只剩月牙形状和触感反馈还生效,桌面小组件和 App 内颜色不受影响、和形状同时生效
- **已验证:锁屏小组件槽位容不下 5 个独立满足最小点击面积的图标,这是确定的限制,不是猜测。** 苹果人机界面指南(HIG)"Widgets"页面的 Specifications 表格公布了各类 accessory widget 在每种 iPhone 屏幕尺寸下的精确点数(pt)尺寸,单位是"点"(point,详见下方术语说明),不是像素:
  - 唯一可能横排多个图标的形态是 **accessoryRectangular(锁屏矩形槽位)**:最大机型(iPhone 15/16 Pro Max 等,430×932pt 屏幕)是 172×76pt;常见机型(iPhone 14/15/16 等,390×844pt / 393×852pt 屏幕)是 160×72pt;较小机型(iPhone SE2/8 等,375×667pt 屏幕)是 153×68pt
  - 5 个图标横向平分这个宽度:172÷5≈34.4pt(最大机型)到 153÷5≈30.6pt(最小机型)——**在所有当前 iPhone 机型上都低于苹果建议的最小点击面积 44×44pt**,哪怕用尺寸最大的 Pro Max 也只能到约 34pt,缺口不是靠"选大屏机型"能绕过去的
  - **accessoryCircular(圆形槽位)** 直径只有 68–76pt,整个槽位都放不下 5 个独立点位,更谈不上细分
  - **accessoryInline(锁屏时钟上方的文字行槽位)** 苹果官方文档明确说明它"只提供一个点击区域"("inline accessory widgets offer only one tap target"),直接排除多点位方案
  - 结论:**5 个独立可点击的月牙图标无法原样塞进锁屏 Widget 槽位、同时满足苹果推荐的最小点击面积**,这条限制已从"待验证的风险"变成"已确认成立的架构性限制"
  - (技术备注:44×44pt 是苹果的设计建议,不是系统强制拦截——理论上可以做出小于 44pt 但仍能响应点击的按钮,只是点击区域越小,用户实际"点不准"的概率越高,这和第 7 节想规避的误操作风险本质上是同一类问题的另一面,不建议为了凑够 5 个点位牺牲这条建议)
  - 原定的"待办:让 Claude Code 做原型实测尺寸"已不再需要——苹果官方文档已给出精确数字,不用再靠原型反推
  - **已拍板:锁屏改为 3 档粗粒度(难过 / 一般 / 开心),细分的 5 档(含两端"很难过""很开心")只保留在桌面小组件和 App 内。** 锁屏 accessoryRectangular 槽位宽度分 3 份,每份能轻松超过 44pt,不再有点不准的风险;代价是锁屏无法直接记录两端的极端心情,需要多点一步打开桌面小组件或 App——这个代价可以接受,因为锁屏定位本来就是"日常快速一瞥"的轻量入口,真正情绪剧烈波动、需要精确记录到"很难过/很开心"的场景,用户多半也愿意多点一步。数据结构不受影响(第 9 节的 `moodLevel` 依然是 1–5 的五档枚举,锁屏只是少放了两个按钮,不是新增一套三档的编码)
  - **桌面小组件同步改为长矩形(Medium)布局,不再用正方形。** 桌面小组件不受锁屏那条"强制单色"和这条"槽位过窄"的限制——Medium 尺寸大约 364×170pt(以最大机型为例),5 个图标横排每个能分到 70pt 以上的宽度,不拥挤,可以在桌面小组件上继续保留完整的 5 档
- **新发现:交互式 Widget(锁屏和桌面)点击目前没有官方支持的震动反馈 API。** 苹果开发者论坛(Apple Developer Forums)上开发者验证过,标准的 `UIFeedbackGenerator` 震动在 WidgetKit 的交互式 Widget 点击场景下不生效,官方文档也没有提供专门用于 Widget 扩展进程触发震动的 API;个别 App 有用系统级的 `AudioServicesPlaySystemSound`(粗颗粒度的整机震动,不是精细的 Taptic 反馈模式)这类非正式手段绕过去,但不是文档化的支持方案
  - 结论:第 8 节设计的"触感反馈"这条区分线索,**只能在 App 内(月牙选择器、软糖圆球)可靠触发标准震动**;锁屏和桌面小组件点击大概率没有震动反馈,或者只能做到不精细的整机震动,不要在锁屏/桌面场景的设计上依赖精细触感反馈作为区分手段
  - 待办:开发时用真机分别测试锁屏、桌面小组件点击是否有任何震动效果(哪怕是粗颗粒度的),确认现状后再决定要不要花额外工程量接入 `AudioServicesPlaySystemSound` 这类非正式方案

## 7. 隐私、数据存储与误操作处理原则

- 所有心情数据默认仅存储在设备本地(SwiftData / CoreData),不上传任何服务器
- **误操作处理机制:已确定采用方案 A(取消确认门槛),并叠加一层轻量的本地异常检测作为兜底。** 原"逐条确认/撤销"方案已废弃,原因:该方案对只用锁屏记录、很少打开 App 的用户会造成"待办堆积"的负担,与第 1.1 节"前端尽量简化"的原则相悖
  - **基础机制:不设确认门槛。** 锁屏来源的记录直接和 App 内记录一视同仁地存入历史,不设任何"待确认"状态,不阻塞任何图表计算。防误操作的默认手段是被动纠错——历史列表支持滑动删除,发现某条记录不对劲随手删掉即可,不设专门的审核步骤。心情记录属于低风险数据,不值得为低概率的误触发场景给所有正常使用场景增加心智负担
  - **异常检测规则(纯本地规则判断,不涉及网络或 AI 服务):短时间内出现多次、跨度很大的心情点击,标记为疑似异常。** 具体判定:同一来源(通常是锁屏)在 60 秒内产生 ≥3 条记录、且这些记录里出现了 ≥2 个不同的心情档位,就把这一批记录标记为 `flaggedAnomalous = true`——典型场景是手机放口袋里被蹭到、被小孩或旁人乱点。这个判断在写入数据的时候本地实时计算,不需要额外的后台任务或网络请求
  - **标记之后不做任何阻断,只做被动提示。** 被标记的记录照样正常存入历史、正常参与图表统计(不因为疑似异常就从数据里剔除,毕竟也可能真的是短时间内心情剧烈波动),只是下次用户打开 App 时,历史列表顶部出现一条可随手划掉的提示条("检测到 N 条心情记录在短时间内变化异常,是否需要看看"),点进去能直接看到这几条被标记的记录、方便一键批量删除或逐条核对;用户不理会这条提示,数据也不受任何影响,不阻塞使用
  - 阈值(60 秒 / ≥3 次 / ≥2 个不同档位)是初始估算值,正式开发后建议用自己的真实使用习惯校准,避免正常的"心情快速反复"被误判
  - **新增:10 分钟内同一档位的连续重复点击,自动归并为一条记录,不逐条建档。** 这是针对实际使用中出现的"报复性解压式重复点击"场景——用户情绪激动时反复点同一个心情按钮当解压出口,不是想记录很多条相同的心情。判定:同一来源在 10 分钟内连续出现同一个 `moodLevel`,只保留第一条记录(时间戳不更新),期间的重复点击不新建 `MoodEntry`,只在交互上正常响应(震动/动画反馈照常触发,让用户感觉每次点击都有回应),避免历史记录和图表被大量重复的同档位记录污染。这条和上面"异常检测"规则是同一类"防止真实数据被无意义点击稀释"问题的两个方向:异常检测抓的是"短时间内出现不同档位"(疑似误触),这条抓的是"短时间内重复同一档位"(疑似解压式点击),两条规则独立判断、互不冲突。这也是 v13 被否决的"多次点击表示强度"方案里提到的"担心用户为解压反复点同一按键"这个风险在真实使用中的验证,当时选择不做"点击计数编码强度",现在选择"直接归并、不编码",思路是一致的:点击次数从不应该是数据的一部分
- 若后续加入 LLM 周报功能,需要明确告知用户:生成报告时会将结构化摘要(不含原始文字备注,除非用户同意)发送给 AI 服务
- App Store 隐私标签(Privacy Nutrition Label)如实填写

## 8. 交互形状、视觉风格与配色方案

- **心情选择器交互形状:5 个"月牙叠加色块"的图标排成一排**(不是纯色圆点,也不是纯色月牙,是两者叠加成同一个图标),轨道整体画成一条从左下到右上的弧线(左边位置低、右边位置高),用"情绪高涨/低落"这个大家共通的空间隐喻做二次强化;点击选中,选中态用轻微放大+外圈光晕反馈
- **统一图标结构(三端共用同一套设计,不再是"锁屏一套、桌面/App 一套"两套方案)**:每个图标的底层还是第 8 节原本的双极配色圆点(色值不变),月牙形状是叠加在颜色圆点**之上**的一层不透明遮罩(用一个偏移的同背景色圆形"啃掉"一部分,即 `crescent_icons.png` 里验证过的做法),露出的部分依然是原来的颜色。这样同一份图标资源在不同场景下自动呈现出不同的信息层次,不需要为锁屏和桌面/App 分别设计两套视觉:
  - **锁屏**:系统强制去色、只剩单一色调渲染,颜色这层编码自动失效,但月牙的**形状轮廓**不依赖色相,依然能被看出是哪一档,这是锁屏场景下唯一可靠的编码方式
  - **桌面小组件、App 内**:不受去色限制,月牙形状和底层颜色同时可见,是"形状+颜色"双重编码,比单一颜色更稳健(比如色弱用户、强光下看不清颜色的场景,形状依然是备用线索),也顺带满足了下面提到的"全球心情"类功能对颜色的需求
  - 好处:开发只需要维护一套图标生成逻辑(颜色 + 遮罩偏移量两个参数决定最终样子),不需要写"锁屏用月牙组件、桌面用圆点组件"两套 UI 代码;好处也包括未来万一想调整锁屏方案(比如真的放不下 5 个点位、要拆成两个 Widget),核心图标资源不用重做
- **非颜色的区分线索(锁屏场景下靠这些补足颜色缺失,桌面/App 内是锦上添花)**:
  - **固定空间顺序**:5 个图标永远按同一顺序排列(左难过、右开心),用户建立一次"位置=心情方向"的认知后,不再需要依赖颜色或文字识别
  - **弧线轨道的高低起伏**:整条轨道画成上文所述的上扬曲线,強化"越高越正面"的直觉
  - **月牙厚薄**:两端(很难过、很开心)最薄、越靠近"一般"越饱满、"一般"是实心圆,用形状厚薄传达"情绪强度"
  - **触感反馈**:点击最极端的两档(很难过、很开心)时用稍强的震动反馈,中间档位用轻一点的震动,作为一个完全不占用视觉资源的确认渠道,尤其适合锁屏"快速一瞥"的场景
  - 已产出对比示意图 `lockscreen_vs_homescreen.png`(锁屏单色 vs 桌面/App 全彩)和 `crescent_icons.png`(月牙厚薄在单色/全彩两种渲染下的效果),直观展示统一图标在不同场景下的呈现差异
  - **新发现并已拍板:纯月牙形状编码的可学习性不够,用户第一次看不出哪个月牙代表开心、哪个代表难过。** 形状编码本身不放弃——锁屏强制单色的限制依然成立,形状仍然是锁屏场景下唯一可靠的区分手段;但锁屏是唯一真正受"空间小 + 强制单色"双重限制的场景,桌面小组件(全彩、Medium 尺寸空间宽裕)和 App 内(空间不受限)都没有这两个限制,之前为了三端视觉统一把文字标签也一起去掉了,属于过度统一。改法:**锁屏保持纯形状 + 触感,不加文字;桌面小组件和 App 内在图标下方恢复显示文字档位标签**(很难过/难过/一般/开心/很开心),变成"形状 + 颜色 + 文字"三重编码,新用户可以在桌面/App 内先靠文字认清每个月牙对应哪一档,建立"位置+形状=心情"的认知后,再到锁屏场景下纯靠形状识别的学习成本会明显降低——这也是第 8.1 节图标下的一致做法,不是新引入的视觉元素
- **数据层与视觉皮肤层分离**:心情按钮的底层永远是第 9 节的 `moodLevel` 数值决定颜色和月牙厚薄这两个参数,不做自由涂鸦、不支持照片替代——因为第 8.1 节的月度色块是靠"心情档位对应的颜色数值做加权平均"算出来的,只有系统化的颜色编码才能参与这种计算;涂鸦/照片这类自由视觉不能被"平均",一旦用它们替代颜色,月度汇总图表的机制就会失效。涂鸦、照片作为装饰性"皮肤"叠加在图标之上(不替代颜色和月牙形状)的想法,放进了第 5.1 节作为远期功能
- 走极简/抽象路线,不使用五官俱全的拟人化表情(哭脸/笑脸的具体表情、卡通形象),月牙造型取的是"嘴角弧度"这一个最抽象的联想,不画眼睛嘴巴等其他五官,克制程度上仍然比表情包类图标高很多
- 明确不做的方向:装饰性贴纸、插画角色、照片拼贴日记风格(如参考截图中的 Moment Recorder 类 App)——**唯一例外见下方"情绪互动装置"一条,范围严格限定,不代表整体方向改变**
- **新增:App 内主页的情绪互动装置("软糖圆球")。** 这是本 MVP 唯一一处主动引入的装饰性/把玩向元素,决定采用前先说清楚它的边界,避免和上面"不做拟人化/装饰角色"的原则互相打架:
  - **是什么**:主页上一个大号的球体,材质做成软糖/果冻般的光泽质感(和月牙图标一样是"圆"的变体,只是渲染质感不同,不是新的几何形状);颜色跟随当前(最近一次)记录的心情档位,直接复用第 8 节的双极配色——球本身**不画眼睛嘴巴等任何五官**,不是一个"角色"或"吉祥物",只是一个会变色的把玩物件,这是它和 Reflectly/MOODA 那类插画角色的关键区别
  - **交互目的**:记录心情之后,用户如果愿意点进 App,可以看到、把玩这个球(比如按压回弹的动画反馈),提供类似解压玩具的触感满足;它是**记录之后的正反馈/奖励层**,不是记录本身的操作入口
  - **严格的范围边界(三条,任何一条都不能突破)**:
    1. **只出现在 App 内主页,不出现在锁屏和桌面小组件。** 第 6 节验证过的锁屏强制单色渲染限制依然成立,球体是纯色实心,一旦去色就没有任何区分度,所以球体天生不适合锁屏场景;锁屏/桌面小组件继续使用月牙图标,不受这次改动影响
    2. **心情记录的输入方式完全不变,还是月牙造型的 5 档选择器。** 球只是"选完之后额外看到的东西",不能替代、也不能新增一种记录心情的方式
    3. **把玩球体不产生任何新的 `MoodEntry` 记录,也不能修改已经记录的心情档位。** 它只读取当前值来决定自己的颜色,是纯粹的"皮肤层/反馈层",不参与写入——这一条特别重要,因为本 PRD 之前讨论过"反复点击容易变成解压式无意义操作、污染数据"的风险(见第 7 节的思路),球体的按压反馈必须做成纯视觉/触感效果,不能每按一次就悄悄多记一条数据
  - 需要和实际 Swift 代码对一遍:目前 ContentView.swift 里主页下方的 5 档选择器如果还是纯色圆点而不是月牙造型,需要改回月牙造型,以符合"其他都不变"这条决定
  - 软糖圆球目前的材质渲染效果(光泽/果冻质感)和预期有差距,这是实现层面的打磨问题(渲染参数、光影、高光位置等),不是范围或规则的问题,不改变上面三条边界,由 Claude Code 继续迭代材质效果即可
  - **App 内主页底色正式确定跟随当前心情颜色动态变化**(和软糖圆球一起变,强化整体氛围的一致性),但底色的具体取色不能直接照抄第 8 节图标用的心情色原值——因为一旦背景色和某个心情档位的图标色完全相同(比如现在背景和"开心"撞色),会让人误以为"背景颜色也是一种数据信号",破坏"颜色只在图标/图表里承载含义"这条既有原则。做法:背景色在心情色基础上明显降低饱和度和/或提高明度(比如取心情色的浅色调/低饱和版本),让背景始终读作"氛围底色"而不是"另一个图标",同时要保证前景文字和控件在 5 种心情底色下都有足够对比度,正式开发前建议连同第 8 节其余色值一起过一遍可访问性校验
- **配色结构:双极+中性("diverging"配色),而不是单一色系深浅渐变**——冷色(蓝)表示偏负面,暖色(橙)表示偏正面,中间用灰色表示"一般",不用红色表示正面情绪(红色在界面语言里通常代表警示/危险,容易和"心情好"产生认知冲突)
  - 为什么不用"难过深色、开心浅色/亮色"这种单一色系渐变:心情本质上是有正负两极、中间有真实零点的"极性"数据,不是单纯的强弱程度。单一色系只能表达"强烈程度",没法直接传达这份强烈是正面还是负面,读图的人得先记住"深=坏、浅=好"这个约定才能看懂;双色系配色不需要这层转换,冷暖直接对应负面/正面,识别成本更低。这是信息传达效率上的取舍,不只是审美偏好,除非有意愿为了品牌统一性牺牲这点直觉性,否则建议保留双色系
  - 很难过:深蓝 `#1c5cab` / 难过:浅蓝 `#6da7ec` / 一般:中性灰 `#9c9b95` / 开心:浅橙 `#f0a273` / 很开心:深橙 `#eb6834`(深色模式下另有一套对应色值,已在示意稿里实现)
  - 这组色值是方向性的示意,正式开发前建议再跑一遍色彩可访问性校验(色盲安全度、对比度),而不是直接照抄
  - "当天无记录"用浅灰色块表示,和"一般"心情的灰色明确区分开,避免把"没记录"误读成"心情一般"

### 配色的科学依据与备选方案

- "红色让人看久了容易冲动"这类说法,查证后属于**有真实研究支持但被大众简化夸大**的类型:确实有研究发现红色能让人对愤怒表情的识别反应变快(语义层面的启动效应),但同一研究也指出这可能只是红色分散注意力的副作用,不一定是真正的情绪生理机制;不建议把这类说法当作板上钉钉的科学依据来做设计决策,但作为"红色语义上容易和警示/愤怒挂钩、不适合表示正面情绪"的辅助理由是成立的
- 一项覆盖 30 个国家的跨文化颜色情绪关联研究(Jonauskaite et al., *Psychological Science*, 2020)发现:红色在几乎所有文化里都同时和"愤怒"及"爱"挂钩,意义比较模糊、正负两极都沾边,不适合单独代表"很开心";橙色、黄色、粉色则比较一致地和"快乐、愉悦"关联,埃及是唯一"黄色≠快乐"的例外
- 该研究也发现黑色/灰色在很多文化里其实和"悲伤、失望"关联,而不是天然的"中性"——本 PRD 用灰色表示"一般"心情,依据的是数据可视化里"灰色=图表零点/基准"的行业惯例,而不是日常语境的颜色联想,所以设计上必须始终让灰色配合文字图例出现,不能只靠颜色本身传达"中性"这层意思(第 8 节已经这样设计,继续保持)
- 该研究还发现中国文化里白色和丧葬/悲伤有特殊关联(不同于西方"白色=如释重负"的联想)——提醒以后不要用纯白色表示"很开心"这类正面档位,现在用浅橙色没有这个问题
- **保留当前"蓝配橙"方案**,橙色的快乐关联在跨文化研究里证据比较扎实,建议维持。以下两个备选组合记录下来供以后想换视觉基调时参考,不是本版本要做的决定:
  - **蓝配黄**:黄色是研究里"快乐"关联度最强、最一致的颜色,但纯黄色在浅色背景上天生对比度偏低,必须始终配文字标签才看得清楚
  - **粉配蓝**:粉色同样和快乐/喜爱关联,视觉上更柔和,适合更"温柔"的审美方向,但同样有对比度偏低的问题,且可能带有偏"可爱/女性向"的气质联想,如果想保持中性调性,橙色比粉色更安全
- **MVP 不做用户自定义配色。** 原因:一是自定义配色如果没有做好可访问性校验,容易出现颜色难以区分或者对比度不够的问题,增加开发和测试成本;二是固定配色能让你自己在长期使用中形成"看到这个颜色就知道是什么心情"的肌肉记忆,过早引入自定义反而打断这种识别习惯。可以作为后期版本的一个可选项(比如提供 2-3 套预设配色主题给用户选,而不是完全开放的调色盘),但不纳入本版本范围。

## 8.1 月度图表方案(解决多次记录导致拥挤的问题)

- **月视图用日历热力图,每天只用一个色块概括当天整体心情**,不会因为一天记录 3-4 次就把格子挤爆;色块右上角一个小数字角标提示当天记录条数(只有 1 条时不显示角标,减少视觉噪音)
  - **色块的颜色不是单一平均值,而是"当天最低档位 → 最高档位"的线性渐变。** 原因:如果一天里记了两条完全相反的心情(比如一次"很难过"+一次"很开心"),算术平均值会算成 3("一般"),这会和一个真的心情平静、全天没有波动的日子染成一模一样的颜色——一个情绪剧烈起伏的日子和一个真正平静的日子被画成相同的视觉结果,是这张图会说谎的地方,"点进去能看到当天详情"不能完全补救这个问题,因为用户压根不会意识到需要点进去看。改成取当天最低档位和最高档位两个颜色做渐变后,只有一条记录、或者当天所有记录档位相同的日子,渐变两端颜色相同,会自动退化成和纯色完全一样的效果,不影响绝大多数日子的观感;只有真正大幅摆动的日子才会出现明显的跨色渐变,不用点进去就能一眼看出"这天不平静"
- **本月趋势线**:把每天的平均值连成折线,以"一般"为基准线,线在基准线上方用暖色浅色块填充、下方用冷色浅色块填充,一眼看出整个月偏正面还是偏负面;当天没有任何记录的日子,直接在折线上留空,不编造插值数据
  - **每个点上额外加一条"当天最低→最高"的细竖线(类似股票 K 线图的高低线)。** 平时波动小的日子这条线很短甚至看不出来,只有像上面举例的那种大幅摆动的日子才会明显拉长——和热力图的渐变改法是同一个思路:把"平均值"变成"平均值 + 范围",不掩盖极端值共存的事实
- **点击某一天,展开当天的时间线视图**,这时候才把当天每一条记录按实际发生的时间点铺在一条 24 小时横轴上,配合 hover/点击提示具体时间和心情——这一层才是真正体现"一天内心情波动"的地方,因为只需要处理一天的数据量,不会拥挤
- **本月心情分布直方图**:把这个月所有单条记录按 5 个档位分类计数,画成 5 根柱子,柱子颜色沿用对应档位的颜色。它和热力图/趋势线是互补视角——后两者按天概括,容易把偶尔出现的极端值(比如穿插的几次"很难过")稀释掉,直方图能把这些原始次数如实呈现出来
- **(可选,深度视图)心情时刻分布散点图**:横轴日期、纵轴一天 24 小时,每一条原始记录按实际发生的时间点单独打一个点,不做任何按天概括——能看出"哪天情绪起伏大""哪个时间段情绪有规律"这类被平均值抹掉的信息。因为把一个月所有记录都摊开会明显更拥挤,定位成一个可选的第三层入口(比如"查看本月所有记录时间点"),不放进日常一打开就看的首屏,不影响热力图/趋势线的简洁性
- 上述改动只涉及"图表怎么画",不改变第 9 节的数据结构、不新增字段——渐变端点、高低线、散点图都是从已有的 `moodLevel` 和 `timestamp` 现算现画
- 已产出可交互 HTML 示意稿:mood_chart_mockup.html(热力图/趋势线/分布直方图/点击展开时间线的完整组合)、mood_heatmap_full_month.html(热力图单独示意)、mood_trend_month.html(趋势线单独示意)、mood_time_scatter_month.html(心情时刻分布散点图示意)、mood_heatmap_average_problem.html(平均值掩盖起伏的问题与渐变改法对照),可以直接打开体验

## 8.2 二级页面视觉统一与导航改版

- **问题:设置/历史/趋势这几个二级页面目前视觉风格偏系统默认样式("像苹果自带 App"的感觉),和主页(月牙选择器+软糖圆球)的视觉语言不统一,整体观感割裂。**
- **已拍板:做一次覆盖全部页面的统一视觉规范,而不是只改导航入口。** 需要梳理出一套贯穿全 App 的设计 token(配色——已在第 8 节定义、圆角、间距、字重、图标风格),二级页面的卡片、列表项、按钮等基础组件都按这套规范重做,不是零散地在个别页面上加点风格化装饰
- **导航入口从文字改为图标形式,集中放置。** 目前"设置""历史""趋势"这类入口是文字列表项,改为统一风格的图标按钮,集中放在一处(比如主页顶部或底部一个图标栏),而不是分散的文字入口;图标需要保留可访问性文字标签(VoiceOver 朗读用),只是视觉上不再默认展示大段文字,不是完全去掉文字信息
- 这一项工作量比单独的第 6/7 节的规则调整大,涉及多个页面重做,建议作为一个独立的开发批次推进(见第 12 节开发顺序),不和数据层/规则类改动混在一次改动里
- App 内不再重复显示"MoodLock"这个 App 名称字样——系统主屏幕图标下方已经展示过一次 App 名称,App 内部再展示是冗余信息,直接去掉

## 9. 数据结构草案

```
MoodEntry
- id: UUID
- timestamp: Date
- moodLevel: Enum (1...5, 五档心情等级)
- note: String? (可选文字备注;v21 起是记录之后唯一的深度补充字段,`audioURL` / `transcribedText` 两个语音相关字段已随语音模块一起移除)
- source: Enum (lockScreenWidget / homeScreenWidget / inApp)
- flaggedAnomalous: Bool (默认 false;第 7 节的本地异常检测规则命中时置为 true,仅用于历史列表顶部的被动提示条筛选,不影响记录本身的存储和图表统计)
- skinImageURL: URL? (第 5.1 节远期功能;MoodEntry 表里的字段,不是单独的表;仅在用户手动为这条记录单独选择照片时写入,不做自动快照)
- doodleData: Data? (第 5.1 节远期功能;同上,仅存用户手动为这条记录添加的涂鸦装饰,不做自动快照)

UserSettings
- moodLevelSkins: Dictionary? (第 5.1 节远期功能;"档位 → 当前皮肤素材"的映射,可随时更新当前值,类似微信背景图;不是表,是设置里的一份数据)
```

## 10. 成功标准

- 我自己能连续使用 2 周以上不觉得麻烦
- 锁屏/Widget 记录耗时 < 2 秒
- 5 档心情的实际记录分布不应严重偏向两端(如果发现大家还是只用最开心/最难过两档,说明档位设计或引导有问题,需要重新审视)
- （可选量化）记录频率相比"打开 App 记录"的旧方式有明显提升

## 11. 待确认的开放问题

> 这里只保留**当前真正需要拍板、正文里还没有确定答案**的事项。一旦某项确定下来,就把决策和理由
> 写进正文对应章节,并把这一条从本节直接删除——不在这里用删除线保留"曾经问过什么",完整的历史
> 演变过程由文档最上方的版本变更记录负责,这里只做"现在还欠一个决定"的清单,避免和已解决事项混在一起。

- 暂无待决事项。

## 12. 开发顺序建议(第 6 节风险已结案,进入开发阶段)

> 原则:一次只推进一个小功能点,做完验收满意再进入下一项,不要让 AI 一次性把 MVP 清单全部生成。
> 每完成一项,回到第 4 节把对应复选框打勾。

1. **数据层 + 基础交互**:实现 MoodEntry 的本地存储(SwiftData),App 内 5 个圆形心情按钮(第 8 节配色方案),点击后写入一条记录,做一个最简单的历史列表页能看到记录。这一步验证的是最核心的数据模型对不对,做完自己先用几天。
2. **锁屏 / 桌面 Widget**:按第 6 节确认的方案——锁屏 3 个固定点位分段点击(难过/一般/开心),桌面小组件用 Medium 长矩形布局放完整 5 档,均通过 AppIntent 触发写入,不做连续滑动。这是工程量和踩坑概率最高的一步,建议单独留出更多调试时间,并且需要真机测试(模拟器对 Widget 交互的支持有限,尤其是第 6 节提到的震动反馈现状需要真机验证)。
3. **异常检测与被动提示**:实现第 7 节的两条本地判定规则——60 秒内 ≥3 条记录且 ≥2 个不同档位标记 `flaggedAnomalous`,以及 10 分钟内同一档位连续点击自动归并为一条记录;以及历史列表顶部的被动提示条 UI,不做阻断性的确认流程。
3.1 **App 内视觉打磨(独立批次,不和上面的规则/数据层改动混在一起)**:软糖圆球材质迭代、主页底色跟随心情动态变化(第 8 节配色差异化规则)、桌面/App 内图标下恢复文字标签(第 8.1 节)、App 图标设计。
3.2 **二级页面统一视觉改版(独立批次)**:按第 8.2 节的设计规范重做设置/历史/趋势页面,导航入口改图标形式集中放置。
4. **补充记录能力**:文字备注(v21 起是唯一的深度补充手段,语音录音模块已移除);历史列表继续查询全部原始记录,只是在 `note != nil` 的行上加备注图标角标 + 预览文字做突出显示(v22 修正,不是过滤)。
   - 实际删除 `AudioRecorder.swift` / `AudioPlayer.swift` / `SpeechTranscriber.swift` 这三个文件,以及麦克风权限声明,建议在 Xcode 里用"删除文件"操作完成(会同步清理 `project.pbxproj` 里的文件引用),不要在文件系统里直接删文件本体,否则容易留下悬空引用导致编译报错
5. **图表:热力图 → 趋势线 → 分布直方图 → 时间线详情**,按第 8.1 节的四个部分依次实现,可以直接把 mood_chart_mockup.html 的交互逻辑作为参考交给 Claude Code 移植成 SwiftUI/Swift Charts 实现。
6. **MVP 收尾**:走一遍第 10 节成功标准,自己连续使用观察数据是否符合预期(比如 5 档分布是否偏向两端)。
7. **MVP 稳定后,再考虑第 5 节的差异化功能**(优先 AI 周报洞察,其次 HealthKit,watchOS 作为远期),第 5.1 节的个性化皮肤层同理往后放。

---
*最后更新:2026-09-01(v22)*
